<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ISEE2019_CODEPROS_team.MoneyCTRL">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".XTR_CLASSES_Constants.InitialSetup"></activity>
        <activity android:name=".View.CategoricalChart" />
        <activity
            android:name=".View.AddNewTrans"
            android:parentActivityName=".View.TransView" />
        <activity android:name=".View.InitailSetup" />
        <activity android:name=".View.Register" />
        <activity
            android:name=".View.TransView"
            android:launchMode="singleTop" />
        <activity android:name=".View.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>