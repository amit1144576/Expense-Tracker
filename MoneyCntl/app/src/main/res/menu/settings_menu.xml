<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/set_currency"
        android:icon="@drawable/ic_currency"
        android:title="Currency"
        app:showAsAction="never" />

    <item android:id="@+id/change_password"
        android:icon="@drawable/ic_currency"
        android:title="Change Password"
        app:showAsAction="never" />

    <item android:id="@+id/view_statistics"
        android:icon="@drawable/ic_currency"
        android:title="Statistical Charts"
        app:showAsAction="never" >

        <menu>

            <item android:id="@+id/categorical_charts"
                android:icon="@drawable/ic_currency"
                android:title="Categorical"
                app:showAsAction="never" />

            <item android:id="@+id/income_expense"
                android:icon="@drawable/ic_currency"
                android:title="Income graph"
                app:showAsAction="never" />

        </menu>


    </item>


    <item android:id="@+id/set_exit"
        android:icon="@drawable/ic_currency"
        android:title="Exit"
        app:showAsAction="never" />

</menu>